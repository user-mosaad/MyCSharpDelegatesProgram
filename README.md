# MyCSharpDelegates

Delegates let you call multiple functions at once. So, for this project, I used a delegate to call 3 methods that update stuff. Then I just called (invoked) the delegate once while passing any parameters needed.
